version: '3.8'

services:
  webapp:
    build:
      context: .
      dockerfile: WEBAPP.Dockerfile
    ports:
      - 53907:5000
  # TODO(netux): create Dockerfile for ROBOT
  # robot:
  #   build:
  #     context: .
  #     dockerfile: ROBOT.Dockerfile
